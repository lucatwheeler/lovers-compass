import SwiftUI

/// ContentView simply forwards to RootView.
/// This file exists because Xcode project templates expect it.
struct ContentView: View {
    var body: some View {
        RootView()
    }
}

#Preview {
    ContentView()
}
